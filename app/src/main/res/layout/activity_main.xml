<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.MainActivity">

    <FrameLayout
        android:id="@+id/fragment_place"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@+id/bottom_main_menu"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_main_menu"
        android:layout_width="match_parent"
        android:layout_height="@dimen/bottom_main_menu_height"
        android:elevation="2dp"
        android:background="@drawable/bottom_rectangle"
        app:itemIconTint="@color/color_text_bottom_menu"
        app:itemTextColor="@android:color/darker_gray"
        app:itemRippleColor="@color/active_button_bottom_menu"
        app:itemTextAppearanceActive="@style/bottom_nav_text_active"
        app:itemTextAppearanceInactive="@style/bottom_nav_text_no_active"
        app:labelVisibilityMode="labeled"
        app:layout_constraintBottom_toBottomOf="parent"
        app:menu="@menu/bottom_main_menu" />


</androidx.constraintlayout.widget.ConstraintLayout>